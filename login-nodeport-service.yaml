apiVersion: v1
kind: Service

metadata:
  name: login-nodeport

spec:
  type: NodePort

  selector:
    app: login

  ports:
    - port: 80
      targetPort: 3000
      nodePort: 30080

#curl -sL http://127.0.0.1:30080/login
