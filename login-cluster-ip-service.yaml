apiVersion: v1
kind: Service

metadata:
  name: login-service
  labels:
    app: login

spec:
  type: ClusterIP

  selector:
    app: login

  ports:
    - port: 80
      targetPort: 3000

#wget -qO- http://login-service/login